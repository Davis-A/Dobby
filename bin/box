#!/usr/bin/env perl
use v5.36.0;

# PODNAME: box
# ABSTRACT: your box-managing buddy

use lib 'lib';
use lib '/Users/rjbs/fm/code/Synergy/lib';
use lib '/Users/rjbs/fm/code/Dobby/lib'; # debugging stuff
use lib '/Users/rjbs/fm/code/hm/modules'; # Yakker is here

use Boxmate::App;

binmode *STDOUT, 'encoding(utf-8)';
binmode *STDERR, 'encoding(utf-8)';
binmode *STDIN, 'encoding(utf-8)';

Boxmate::App->run;
